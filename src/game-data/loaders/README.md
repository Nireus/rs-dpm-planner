# Loaders

Pure loading entry points for curated game-data documents.

These functions accept file or text loaders from the outside so the game-data
layer stays independent of Angular and browser APIs.
