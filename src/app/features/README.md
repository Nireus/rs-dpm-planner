# Features

Feature-oriented Angular UI areas live here.

Each feature should focus on user workflows and presentation while delegating
domain logic to `src/simulation-engine` or curated definitions in
`src/game-data`.
