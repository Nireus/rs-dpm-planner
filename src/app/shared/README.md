# Shared

Reusable UI building blocks and helper utilities for the Angular app layer.

Examples:

- presentational components
- reusable directives and pipes
- UI utilities

Do not place combat simulation rules here.
