# Validation

Strict validation rules and structured issue generation for invalid plans.

This area also contains runtime validation for portable import/export documents.

Current engine validation coverage includes:

- timeline bounds
- strict rotation-plan legality checks
- player stat validation
- portable import/export validation
