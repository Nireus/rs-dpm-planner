# Resolvers

Pure simulation-state resolvers that evolve per-tick state without Angular dependencies.

Current foundation:

- adrenaline timeline resolution
- per-tick adrenaline state snapshots
- illegal-use blocking for insufficient adrenaline
- channel occupancy resolution
- completion-only and multi-hit channel state support
- cooldown timeline resolution
- per-tick cooldown state snapshots
